name: main

on:
  workflow_dispatch:

jobs:
  build:
    name: test1
    runs-on: ubuntu-latest
    steps:
      - name: Build
        run: echo building

  deploy:
    name: deploy
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: macnev2013/manual-approval@v1.0.18
        with:
          secret: ${{ github.TOKEN }}
          approvers: albionh
          minimum-approvals: 1
          approval-wait: 600
          issue-title: 'Manual Approval'
          issue-body: |
            "Issue Body"
